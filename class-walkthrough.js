$('document').ready(function() {
var array = [colors];

$('.welcome').append('welcome you have pick'  + color + '!');
var randomNumber;
var colorArray = [];




























function randomNumber(min, max){
    return Math.floor(Math.random() * (1 + max - min) + min);
}

function choose() {
	var number = randomNumber(0,4);
} 


});